/**
 * @author ntishkevich
 */
var sheepCount = 0;
while (sheepCount < 9) {
    sheepCount++;
}
alert(sheepCount);

var sheepCount = 0;
do {
    sheepCount++;
} while (sheepCount < 9);
alert(sheepCount);

for (var sheepCount = 0; sheepCount < 9; sheepCount++) {
    // empty body
}
alert(sheepCount);
